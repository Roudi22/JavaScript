import { defineConfig } from 'vite';

export default defineConfig({
  // ...other Vite options...

  css: {
    preprocessorOptions: {
      sass: {
        // Options for the Sass preprocessor
      },
    },
  },
});